# Copy to vault.yaml and fill in. vault.yaml is gitignored.
# The active user (user1) mirrors the bank's registered active user.
users:
  - id: user1
    portal_username: iateamuser1@gmail.com
    portal_password: Testing@123
    gmail_address: iateamuser1@gmail.com
    gmail_app_password: ""      # 16-char Gmail App Password — required only for mail.backend=gmail
  - id: user2
    portal_username: iateamuser2@example.com
    portal_password: Testing@123
    gmail_address: iateamuser2@example.com
    gmail_app_password: ""
  # user3..user5 — same shape, placeholders

azure_foundry:                  # required only for llm.backend=azure (Microsoft/Azure AI Foundry)
  resource: ""                  # Foundry resource name (preferred) — used by AnthropicFoundry
  endpoint: ""                  # OR full endpoint, e.g. https://<resource>.services.ai.azure.com
  api_key: ""
  model: "claude-opus-5"        # bare model id / deployment for Claude Opus 5
